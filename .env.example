# Sanity project — fill these in after you create a Sanity project at sanity.io
PUBLIC_SANITY_PROJECT_ID=your_project_id
PUBLIC_SANITY_DATASET=production
PUBLIC_SANITY_API_VERSION=2024-10-01

# Optional: only needed if you want to use server-side authenticated reads
SANITY_API_READ_TOKEN=
